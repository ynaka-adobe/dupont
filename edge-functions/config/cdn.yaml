kind: 'CDN'
version: '1'
data:
  originSelectors:
    # Personalization moved to the client-side target-slot-hero block, so no
    # pages are routed through the edge function any more. Empty = serve every
    # page normally from the Edge Delivery CDN (fully cached, no edge hop).
    rules: []
